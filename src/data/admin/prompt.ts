export const prompt = {
  generate_meta_keywords:
    "for this content created by reactQuill ignore all html tags and create best meta keywords for SEO no duplication and add comma (,) and space ( ) between each keywords not add any text except meta keywords because it will send directly in meta keywords tag",
  generate_meta_description:
    "for this content created by reactQuill ignore all html tags and create best meta description for SEO not add any text except meta description because it will send directly in meta description tag, just send the string value to add in tag",
};
